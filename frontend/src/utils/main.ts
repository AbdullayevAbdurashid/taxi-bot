
export const checkColorScheme = (tg) => {
    if (tg !==undefined) {
      localStorage.setItem("chakra-ui-color-mode", "dark");
    } else {
      localStorage.setItem("chakra-ui-color-mode", "dark");
    }
  };